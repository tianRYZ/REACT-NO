import React, { memo } from 'react'
import IndexmRouter from './router'

const App = memo(() => {
  return (
    <div>
      <IndexmRouter>
      </IndexmRouter>
    </div>
  )
})


export default App